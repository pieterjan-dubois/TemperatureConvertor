<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
                <LinearGradientBrush x:Key="LabelBackgroundBrush" StartPoint="0,7" EndPoint="1,0">
                    <GradientStop Color="Red" Offset="0"/>
                    <GradientStop Color="Blue" Offset="1" />
                </LinearGradientBrush>
            </Window.Resources>
        <StackPanel>
            <TextBlock Text="Celsius" FontWeight="Bold" FontSize="16" Padding="3.5" Background="{StaticResource LabelBackgroundBrush}" />
            <TextBox x:Name="textBox2" FontSize="16" HorizontalAlignment="Center" />
            <Button Content="Convert to Celsius" Click="ConvertToCelsius" FontWeight="Bold" Padding="3.5px" FontSize="16" />
            <TextBlock Text="Fahrenheit" FontWeight="Bold" FontSize="16" Padding="2" Background="{StaticResource LabelBackgroundBrush}" />
            <TextBox x:Name="textBox" FontSize="16" HorizontalAlignment="Center"/>
        <Button Content="Convert to Farenheit" Click="ConvertToFarenheit" FontWeight="Bold"  Padding="3.5px" FontSize="16"/>

</StackPanel>
</Window>
